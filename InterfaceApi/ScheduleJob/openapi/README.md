# OpenAPI（自动生成）

本目录下 `openapi-*.yaml` 由契约接口生成：

```bash
php InterfaceApi/bin/generate-openapi.php --service=ScheduleJob/App
```

模块 `info.title` / `description` 可在 `InterfaceApi/ScheduleJob/openapi-modules.json` 配置。

请勿手工编辑 YAML；变更请改 `Interface/`、`Request`/`Response` 注解后重新生成。
